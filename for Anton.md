Антон, привет!

Что бы хотелось добавить:
- Pytest: тестирование вида как у нас было во время прохождения практики.
- Насколько оптимальна работа database.py и стоит/возможно ли допилить до некоторого подобия Django? Чтобы при обращении к объекту класса и вызове метода, метод возвращал снова объект класса и обращался к базе по типовому методу get (как подобие select).
	Напрмер: urls.filter('id'=5).order('created_at').get('*')